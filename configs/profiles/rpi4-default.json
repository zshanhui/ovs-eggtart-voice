{
  "name": "rpi4-default",
  "description": "Stub profile for Raspberry Pi 4 (CPU-only). 'exclusive' policy is used so ASR and TTS can't both pin all four cores at once.",
  "asr_backend": "cpu.sherpa_asr",
  "tts_backend": "cpu.sherpa",
  "execution_policy": {
    "mode": "exclusive"
  },
  "env": {}
}
