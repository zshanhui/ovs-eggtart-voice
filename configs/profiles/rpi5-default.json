{
  "name": "rpi5-default",
  "description": "Stub profile for Raspberry Pi 5 (CPU-only). RPi 5's faster cores can sustain ASR and TTS concurrently, so 'concurrent' is chosen over RPi 4's 'exclusive'.",
  "asr_backend": "cpu.sherpa_asr",
  "tts_backend": "cpu.sherpa",
  "execution_policy": {
    "mode": "concurrent"
  },
  "env": {},
  "tts_model_id": "sherpa"
}
